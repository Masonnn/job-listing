<div class="col-md-12">
  <div class="job">
    <%= link_to("编辑", edit_job_path(@job), class: "btn btn-primary pull-right")%>
  </div>
  <h1><%= @job.title %></h1>
  <p><%= simple_format(@job.description) %></p>
</div>
